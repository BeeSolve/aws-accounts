---
"@beesolve/aws-accounts": minor
---

Reworked `drift` command to compare your `aws.config.ts` against live AWS state and output ready-to-paste TypeScript snippets showing additions, removals, and modifications needed to bring your config in sync.
